# Версии: что нумеруется и когда поднимается

> Раскладка проекта — [`55-project-layout.md`](55-project-layout.md).
> Порядок сдачи — [`56-static-analysis-and-tests.md`](56-static-analysis-and-tests.md).

**Номер лежит в файле, а не в коде.** Две копии одного числа расходятся, и устаревшей
всегда оказывается та, которую никто не правит.

---

## 1. Два номера, и они независимы

| Файл | Что нумерует | Формат |
|---|---|---|
| [`VERSION`](../../VERSION) | **Библиотека ядра** | `major.minor.patch` |
| [`VERSION_SAVE`](../../VERSION_SAVE) | **Двоичный формат сохранения** | целое число |

**Разделены намеренно.** Формат сохранения переживает много выпусков библиотеки: можно
поднять `VERSION` десять раз, ни разу не тронув раскладку сохранённого состояния. И
наоборот — одна правка, ломающая старые сохранения, должна быть видна сама по себе, а не
растворяться в очередном `0.4.7`.

**Игры здесь нет.** Её пока не существует, и версия у неё будет своя — на стороне UE, по
своей шкале. **Версия ядра — не версия игры.** Когда игра появится, её номер заводится
отдельно и с этими файлами не связывается.

---

## 2. Когда поднимать `VERSION`

**Пока идёт разработка игры, major заморожен на нуле.** Это правило человека, и оно сильнее
всего остального в этом разделе: игры ещё нет, ломающих правок границы впереди много, и
каждая из них поднимала бы номер, у которого нет ни одного внешнего потребителя. Счёт
`2.0.0`, `3.0.0`, `4.0.0` не сообщал бы ничего, кроме того, что мы всё ещё пишем игру.

| Часть | Поднимается, когда |
|---|---|
| **major** | **Не поднимается.** Ноль до выхода игры — решение человека 1 сентября 2026 |
| **minor** | **Сломалась граница ядра и слоя графики** — старый потребитель не соберётся или не заработает. И **сдан этап** плана фазы |
| **patch** | Сдан отдельный модуль или исправление |

**Ноль в major и означает «интерфейс ещё не обещан».** Так его читает и остальной мир:
в `0.y.z` ломающая правка живёт в `y`. Значит, `minor` несёт двойной смысл — сдача этапа и
слом границы, — и различает их не номер, а сообщение коммита.

**Из этого следует правило для потребителя:** слой графики пришпилен к полной строке
`major.minor.patch` (`ue/CORE_VERSION`), а не к одному `major`. Проверять `kCoreVersionMajor`,
пока он заморожен, бессмысленно — он не изменится ни разу.

> **Было иначе, и это исправлено.** 31 августа 2026 при сдаче задачи O2 номер подняли до
> `1.0.0` — «первый выпуск с настоящим интерфейсом для движка». 1 сентября 2026 человек
> отменил это решение по названной выше причине, и номер откатан в `0.9.0`: слом границы
> задачей O2 стал `minor`, каким и будет каждый следующий. Номер `1.0.0` освобождён и
> достанется первому выпуску игры.

**Один бамп на сдаваемый коммит**, не на каждую правку внутри работы над модулем.

---

## 3. Когда поднимать `VERSION_SAVE`

**Только тогда, когда старый файл сохранения перестаёт читаться как есть.**

| Изменение | Бамп |
|---|---|
| Добавили поле в сохраняемую структуру | **Да** |
| **Поле легло в существующий паддинг — `sizeof` не изменился** | **Да, и растяжка на `sizeof` этого не поймает** ([формат §7](../67-save-format.md#7-растяжка-на-sizeof)). Так `FieldRow` получил `LandKind` задачей О2б: размер строки остался 40, а поток вырос на байт на поле |
| Убрали или переставили поля | **Да** |
| Сменили тип поля, ширину, порядок байт | **Да** |
| Изменили состав сохраняемых сущностей | **Да** |
| Переименовали переменную, переписали функцию | Нет |
| Изменили значение по умолчанию в таблице данных | Нет |

**Бамп формата сохранений — событие, как смена интерфейса подсистемы**
(`CLAUDE.md` §8, §10). Он означает, что чьи-то сохранения перестали открываться, и об этом
надо сказать вслух, а не заметить в диффе.

> **Правило чтения:** сохранение с номером **выше**, чем у сборки, **отвергается**, а не
> угадывается. Сборка не знает, что добавили после неё.

**Ниже — тоже отказ** ([формат сохранений §6](../67-save-format.md#6-отказы)): миграций в
первой фазе нет, читать умеем только свой номер. Совместимость со старыми номерами —
отдельное решение, отложенное до появления игры.

---

## 4. Как это видно из кода

CMake читает оба файла и генерирует заголовок:

```
   VERSION       ─┐
                  ├─→  build/generated/include/core_common/version.h
   VERSION_SAVE  ─┘         шаблон: include/core_common/version.h.in
```

```cpp
#include "core_common/version.h"

core::kCoreVersionMajor    // 0 — заморожен до выхода игры (§2)
core::kCoreVersionMinor    // 9
core::kCoreVersionPatch    // 0
core::kCoreVersionString   // "0.9.0" — потребитель сверяется с этой строкой целиком
core::kSaveFormatVersion   // 4
```

| Правило | |
|---|---|
| **Правится файл, а не заголовок** | Сгенерированная копия живёт в `build/` и в git не попадает |
| **Правка файла = перенастройка** | `CMAKE_CONFIGURE_DEPENDS` не даст собрать со старым номером |
| **Формат проверяется** | `VERSION` не `major.minor.patch` или `VERSION_SAVE` не число — ошибка настройки, а не молчаливый мусор |

**Ни хеша коммита, ни времени сборки в заголовке нет.** Они переписывали бы его при каждом
коммите, обнуляли ccache и делали сборку невоспроизводимой — ради сведений, которые и так
берутся из `git log`.

```
  make version    показать оба номера
  make info       они же в общей сводке настроек
```

---

## 5. Чего пока не нумеруем

**Осознанные пропуски, а не забытые.**

| Что | Почему нет |
|---|---|
| **Версия игры** | Игры нет. Версия будет своя, на стороне UE |
| **Версия интерфейса ядро↔UE** | **Решено А1 ([граница §6](../70-boundary.md#6-версионирование--третьего-номера-нет)): третьего файла нет** — граница и есть публичная поверхность библиотеки, её совместимость означает `minor` выше (major заморожен, §2); строки, которые через неё ходят, — `VERSION_SAVE` |
| **Версия формата таблиц данных** | Формат таблиц не выбран (CSV или JSON) — решение открыто, [план §3](../plan/51-phase1-plan.md) |
| Версия отчётов статанализа | Данные служебные, живут вне git |

---

## 6. Бамп — шаг закрытия цикла сдачи

**Версия поднимается тогда, когда закрылся цикл сдачи кода.** Не «когда вспомнили» и не
«если решили»: успешное закрытие и есть то событие, которое номер обозначает.

```
   /check_static_new  ──→  успешный проход  ──→  БАМП  ──→  коммит
                            (§7.3 команды)              одним коммитом
```

| Что закрылось | Что поднимается | Кто |
|---|---|---|
| **Сдача модуля** | `VERSION` **patch** | **Цикл, сам** |
| **Сдача этапа плана** | `VERSION` **minor**, patch обнуляется | **Цикл, сам** — по флагу `--stage` |
| **Сломалась граница с UE** | `VERSION` **minor** | **Человек.** Это не решение цикла, и major при этом не трогается (§2) |
| Раскладка сохранения изменилась | `VERSION_SAVE` | **Человек**, и об этом надо сказать вслух |

```
  make bump-patch    сдан модуль
  make bump-minor    сдан этап плана либо сломалась граница с UE
```

Скрипт — `scripts/bump_version.sh`, он же проверяет форму номера и обнуляет младшие части:
`0.1.3` плюс minor даёт `0.2.0`, а не `0.2.3`. Сдача этапа не наследует счётчик правок
модулей внутри него.

**`bump_version.sh major` отказывает и ничего не пишет** — заморозка из §2 стоит в самом
скрипте, а не только в этом документе: правило, которое соблюдается только на словах,
рано или поздно нарушается молча. Цели `make bump-major` нет.

### Когда бампа НЕ происходит

| | Почему |
|---|---|
| **Досрочная остановка цикла** | Цикл сдачи не закрылся, закрывать нечего |
| **`--once`** | Режим только для чтения, он не правит ничего |
| **Дельта пуста** | Цикл выходит раньше, до сдачи не доходит |
| **Каждая итерация внутри цикла** | Один бамп на закрытие, а не на проход |

### Одним коммитом

**Бамп идёт в том же коммите, что и сдаваемая работа.** Отдельный коммит «bump version»
не заводить: он отвязывает номер от того, что этот номер обозначает, и по `git log` уже не
видно, за что именно номер поднялся.

**После бампа — полная сборка.** Заголовок `core_common/version.h` генерируется при
настройке; артефакт, собранный со старым номером, — ровно та ложь, ради предотвращения
которой номер вынесен в файл.
